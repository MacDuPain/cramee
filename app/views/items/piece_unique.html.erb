
<h1 class="text-center my-4">Pièce Unique</h1>

<h3>Ces pièces étant uniques, elles ne sont peut être pas disponibles à la vente...</h3>
<h3>Par contre, si elles vous plaisent, n'hésitez pas à me contacter pour passer une commande !</h3>

<% if @items.any? %>
  <div class="row">
    <% @items.each do |item| %>
      <div class="col-md-4 mb-4">
        <div class="card h-100 shadow-sm border-0">
          <% if item.image.attached? %>
            <%= image_tag url_for(item.image), class: 'card-img-top', alt: item.name %>
          <% end %>
          <div class="card-body p-4 text-center">
            <h5 class="card-title"><%= item.name %></h5>
            <p class="card-text"><%= item.description %></p>
              <%= link_to "Voir plus", item_path(item), class: "custom-submit-button" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
<% else %>
 <p class="text-center">Aucune pièce unique n'a été trouvée.</p>
<% end %>
